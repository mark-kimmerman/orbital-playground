{
    "name": "orbital-playground",
    "version": "1.0.0",
    "description": "Building intuition in orbital mechanics.",
    "scripts": {
        "build": "rm -Rf dist && mkdir dist && cp src/index.html dist && webpack",
        "start": "npm run build & reload -bd dist",
        "test": "echo \"Error: no test specified\" && exit 1"
    },
    "author": "Mark Timmerman",
    "license": "ISC",
    "devDependencies": {
        "reload": "^3.0.3",
        "webpack": "^4.41.2",
        "webpack-cli": "^3.3.10"
    }
}
